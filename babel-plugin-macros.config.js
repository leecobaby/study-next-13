/**
 * @type {import('twin.macro').Config}
 */
const twin = {
  // add options here
  preset: 'styled-components', // Set the css-in-js library to use with twin
  config: 'tailwind.config.js', // Path to the tailwind config
}

module.exports = {
  twin,
}
