import Infos from '../infos/page'

## BLOG

### TODO

- [ ] Add a blog post about the new `@sveltejs/adapter-static` adapter
- [ ] Add a blog post about the new `@sveltejs/adapter-node` adapter
- [ ] Add a blog post about the new `@sveltejs/adapter-cloudflare-workers` adapter

```js
const svelte = require('svelte/compiler')
const { preprocess } = require('svelte/compiler')
const { preprocess: preprocess2 } = require('svelte/compiler')
```

## 加载组件

<Infos />
